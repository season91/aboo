<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.kh.aboo.admin.schedule.model.repository.ScheduleRepository">
    
    	<select id="selectScheduleList" resultType="schedule">
			select *
			from(
				select rownum rnum, b.*
				from(
					select *
					from tb_schedule
					where is_leave = 0 and apartment_Idx = #{apartmentIdx}
					order by schedule_sdate 
				) b
			) where rnum between #{queryStart} and #{queryEnd}
		</select>
    
    </mapper>